[package]
name = "that-limit-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { workspace = true }
thiserror = { workspace = true }
redis = { workspace = true, features = ["connection-manager", "tokio-comp", "script"] }
